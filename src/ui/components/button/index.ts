import { ButtonRoot } from './button-root';
import { ButtonText } from './button-text';
import { ButtonIcon } from './button-icon';

export const Button = {
  Root: ButtonRoot,
  Text: ButtonText,
  Icon: ButtonIcon,
};

export { EButtonVariant, EButtonSize } from './button.types';
export type { IButtonRootProps, IButtonTextProps, IButtonIconProps } from './button.types';
