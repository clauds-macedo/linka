export enum ERoomStatus {
  LIVE = 'live',
  UPCOMING = 'upcoming',
  ENDED = 'ended',
}

export enum ERoomCategory {
  MUSIC = 'music',
  PODCAST = 'podcast',
  TALK_SHOW = 'talk_show',
  GAMING = 'gaming',
  EDUCATION = 'education',
}

export enum EVideoSource {
  YOUTUBE = 'youtube',
}
